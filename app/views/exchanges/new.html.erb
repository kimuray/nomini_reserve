<div class="container mt-3">
  <h2 class="main-text text-center page-heading mb-4 mt-4">換金申請</h2>

  <%= form_with(model: @exchange, url: exchanges_path, local: true, class: "mr-3 ml-3") do |form| %>
    <% if @exchange.errors.any? %>
      <div class="field form-group signup-form">
        <div class="alert alert-danger">
          <ul>
          <% @exchange.errors.full_messages.each do |message| %>
             <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      </div>
    <% end %>
    <div class="narrow-container">
      <div class="field form-group signup-form">
        <h3 class="text-center current-point mb-4">現在のポイント数: <%= current_user.point_count %></h3>
        <p>
          下記入力フォームより換金したいポイント数を入力してください。<br>
          ※ ポイントの換金は10,000ポイントからとなります。
        </p>
      </div>
      <div class="field form-group row signup-form">
        <%= form.number_field :point, class: "form-control"  %>
      </div>
    </div>

    <div class="actions text-center mb-3">
      <%= form.submit '申請する', class: 'btn main-btn btn-lg', data: { confirm: '申請してもよろしいですか？' } %>
    </div>
  <% end %>

  <%= render 'shared/about_point' %>
</div>
